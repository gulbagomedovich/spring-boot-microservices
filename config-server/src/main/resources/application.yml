server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gulbagomedovich/spring-boot-microservices
          clone-on-start: true
